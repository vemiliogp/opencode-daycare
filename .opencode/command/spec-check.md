---
description: Verifies a spec's acceptance criteria — runs lint/tsc/build, compares screens with the design comps, and marks the spec's checkboxes.
agent: spec-verifier
---

Verify the acceptance criteria of the spec: **$ARGUMENTS**

If the argument above is empty, list the specs in `specs/` and ask the user which one to verify before doing anything else.
